#include <stdio.h>
#include <unistd.h>
/**
*main- Entry
*Description: Print all possible combination of 2 digits
*Return: 0(Success)
*/
int main(void)
{
	int n1 = 48;
	int n2;
	int com = 44;
77;
	while (n1 <= 57)
	{
		n2 = n1 + 1;
		while (n2 <= 57)
		{
			putchar(n1);
			putchar(n2);
			if (n1 != 56 || n2 != 57)
			{
				putchar(com);
				putchar(32);
			}
			n2 += 1;
		}
		n1 += 1;
	}
	putchar('\n');
	return (0);
}
