#include<stdio.h>
/**
 *main-main-entry point for the program.
 *
 *Returns: returns zero if no erros, otherwise returns non-zero value.
*/
int main(void)
{
	printf("with proper grammar, but the outcomeis a piece of art\n");
	return (0);
}
